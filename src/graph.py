"""LangGraph orchestration wiring — Phase 5.

Defines the agent graph:
  story_writer → scene_director → [image_agent || voice_agent] → subtitle_agent
  → compositor → reviewer → (approved? done : retry up to 2x)
"""

from __future__ import annotations

# Phase 5 placeholder.
